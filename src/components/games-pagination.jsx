import React from 'react';
import { GamesPaginationWrapper } from './styled-components/games-pagination-styled-components';

const GamesPagination = () => {
  return <GamesPaginationWrapper />;
};

export default GamesPagination;
